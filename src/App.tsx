function App() {
  return (
    <div className="min-h-svh bg-background text-foreground">
      {/* Add your router here: <BrowserRouter>, <RouterProvider>, etc. */}
      <main className="container mx-auto px-4 py-8">
        <h1 className="text-3xl font-bold">React 19 + Tailwind v4 + shadcn</h1>
        <p className="mt-4 text-muted-foreground">
          Ready for routing. Install react-router-dom and add your routes.
        </p>
      </main>
    </div>
  )
}

export default App
